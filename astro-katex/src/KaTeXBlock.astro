---
import KaTeX, { KaTeXOptions } from "./KaTeX.astro";

export interface Props {
    tex: string, // A Tex Expression
    opts: KaTeXOptions, // KaTeX Options
};

const { tex, opts = {} } = Astro.props;
opts.displayMode = true;
---
<KaTeX {tex} {opts}>
<slot/>
</KaTeX>